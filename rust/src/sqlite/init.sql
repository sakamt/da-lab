CREATE TABLE enkf (
  id PRIMARY KEY,
  K INTEGER NOT NULL,
  r REAL NOT NULL,
  dt REAL NOT NULL,
  tau INTEGER NOT NULL,
  ensemble TEXT NOT NULL,
  truth TEXT NOT NULL,
  observable TEXT NOT NULL
);

CREATE TABLE mpf (
  id PRIMARY KEY,
  K INTEGER NOT NULL,
  T INTEGER NOT NULL,
  M INTEGER NOT NULL,
  r REAL NOT NULL,
  dt REAL NOT NULL,
  tau REAL NOT NULL,
  ensemble TEXT NOT NULL,
  truth TEXT NOT NULL,
  observable TEXT NOT NULL
);
